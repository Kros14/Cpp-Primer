//赋值是会产生引用的一类典型表达式，引用的类型就是左值的类型。也就是说，如果i是int，则表的是i=x的类型是int&。根据这一特点，请指出下面的代码中每一个变量的类型和值。
int a = 3,b =4;
  //a、b都是int类型，值是3、4。
decltype(a) c = a;
  //c是int类型，值为3。
decltype(a = b) d = a;
  //d是int&，绑定到a，值为3。
